
/**
 * # Firestore Security Rules
 *
 * ## Core Philosophy
 * This ruleset enforces a hybrid security model designed for a marketplace application.
 * It combines a strict user-ownership model for private data with a public-read model for shared catalog data.
 * An authenticated user (acting as an admin) has broad permissions to manage users, orders, and the product catalog.
 *
 * ## Data Structure
 * - `/users/{userId}`: Contains private user profile documents.
 * - `/users/{userId}/cart/{cartItemId}`: A user's shopping cart.
 * - `/orders/{orderId}`: All user orders.
 * - `/categories/{categoryId}`: Publicly browsable product categories.
 * - `/subscriptions/{subscriptionId}`: Publicly browsable subscription products.
 *
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions for reusable logic
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    /**
     * Controls access to user profile documents.
     * Grants read/write access to the user for their own document.
     * Grants full read/write access to admins (any signed-in user).
     */
    match /users/{userId} {
      allow get, update: if isOwner(userId) || isSignedIn();
      allow list, delete: if isSignedIn(); // Admin-level access
      allow create: if isOwner(userId);

      /**
       * Controls access to a user's shopping cart.
       * Only the user can access their own cart.
       */
      match /cart/{cartItemId} {
        allow read, write: if isOwner(userId);
      }
      
      /**
       * Controls access to a user's notifications.
       * A user can read/write their own notifications.
       * Admins (server actions) can create notifications for any user.
       */
      match /notifications/{notificationId} {
        allow read, delete: if isOwner(userId);
        allow write: if isSignedIn();
      }
    }

    /**
     * Controls access to all orders.
     * A user can create their own order.
     * An admin (any signed-in user) can read and manage all orders.
     */
    match /orders/{orderId} {
      allow get: if isOwner(resource.data.userId) || isSignedIn();
      allow list, update, delete: if isSignedIn(); // Admin-level access
      allow create: if isOwner(request.resource.data.userId);
    }

    /**
     * Controls access to public subscription categories.
     * Publicly readable, but only admins can write.
     */
    match /categories/{categoryId} {
      allow get, list: if true;
      allow create, update, delete: if isSignedIn(); // Admin only
    }

    /**
     * Controls access to public subscription details.
     * Publicly readable, but only admins can write.
     */
    match /subscriptions/{subscriptionId} {
      allow get, list: if true;
      allow create, update, delete: if isSignedIn(); // Admin only
    }

    /**
     * Controls access to legal pages (Terms, Privacy, etc.).
     * Publicly readable, admin-writable.
     */
    match /legalPages/{pageId} {
        allow get, list: if true;
        allow create, update, delete: if isSignedIn(); // Admin only
    }

    /**
     * Controls access to navigation menu items.
     * Publicly readable, admin-writable.
     */
    match /menuItems/{menuItemId} {
        allow get, list: if true;
        allow create, update, delete: if isSignedIn(); // Admin only
    }

    /**
     * Controls access to global app settings.
     * Publicly readable, admin-writable.
     */
    match /settings/payment {
        allow get: if true;
        allow write: if isSignedIn(); // Admin only
    }
    
    /**
     * Controls access to contact form submissions.
     * Anyone can create (submit the form).
     * Only admins can read, update, or delete submissions.
     */
    match /contactSubmissions/{submissionId} {
      allow create: if true;
      allow read, update, delete: if isSignedIn(); // Admin only
    }
  }
}

    